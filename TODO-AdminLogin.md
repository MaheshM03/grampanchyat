# Add Admin Login Credentials
- [x] 1. Create AdminLogin.jsx page with username/password form
- [x] 2. POST to /api/auth/login on submit
- [x] 3. On success: localStorage.setItem('adminToken'), redirect to /admin
- [x] 4. Protect AdminPanel: check token/session, redirect to login if missing
- [x] 5. Add login link in Navbar
- [ ] 6. Update server auth to use JWT tokens (better than session)

